# Practical Work 4 — Word Count (Mini MapReduce in C++)

This directory contains a simple MapReduce-style implementation of the
classic *Word Count* example.

## Build

```bash
g++ -std=c++17 -O2 -o word_count word_count.cpp
