# Practical Work 3 — MPI File Transfer

This practical upgrades the TCP file transfer system into an MPI-based
file transfer program using C and MPI (e.g. MPICH or OpenMPI).

## Build

```bash
mpicc -o mpi_file_transfer mpi_file_transfer.c
